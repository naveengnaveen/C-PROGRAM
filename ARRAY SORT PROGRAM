
#include<stdio.h>
void main()
{
    int row,colume,i,j;
    scanf("%d \n",&row);
    scanf("%d \n",&colume);
    int num[row] [colume];
    for(i=0;i<row;i++)
    {
        for(j=0;j<colume,j++)
        {
            scanf("%d \n",&num [i] [j]);
            printf("print the matrix:  %d \n",num [i][j]);
        }
    }

}
